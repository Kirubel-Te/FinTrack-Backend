# Copy to .env and fill required values for local development
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
PORT=3000
JWT_SECRET=replace_this_with_a_strong_secret
NODE_ENV=development
